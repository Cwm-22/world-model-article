# 环境与密钥
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# 输出与数据（如需把日报纳入版本控制，可注释掉对应行）
# 注意：GitHub Actions 会把 output/ 下的 md/pdf 提交回仓库归档，
# 所以这里不再整体忽略 output/，只忽略本地构建产物 / 临时文件。
output/_*.png
output/_*.tmp
# 注意：data/processed_papers.json 不忽略——它是已处理论文的去重状态库，
# 必须入仓并在 workflow 每日运行后由 Actions 自动提交更新。
data/*.tmp
output*

# 日志：运行期日志不入仓
*.log
daily_job.log
run.log
run2.log
run3.log
run4.log
regen.log

# 系统/编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
